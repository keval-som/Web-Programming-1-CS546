{{!

If the user is logged in , and 'GET /user' endpoint is called, you will render this view based on the specs.

}}

<main>
  <div id="userView" class="userView">

    <p id="welcome" class="welcome">Welcome
      {{firstName}}
      {{lastName}}, the time is now:
      {{currentTime}}
      and the current date is
      {{currentDate}}. Your role in the system is:
      {{role}}.</p>

    <p id="quote" class="quote">Favorite Quote: {{favoriteQuote}}</p>

    <!-- link here for admin, ONLY if they are an administratoruser <a id="adminLink" href="/administrator">Administrator Page</a> !-->
    {{#if isAdmin}}
      <a id="adminLink" class="link" href="/administrator">Administrator Page</a>
    {{/if}}

    <a id="signoutLink" class="link" href="/signoutuser">Sign out</a>

  </div>
</main>